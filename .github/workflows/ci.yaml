name: "ci"
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn
      - name: Setup font
        working-directory: ./font
        run: yarn
      - run: yarn lint
      - run: yarn test
